# Agent 配置对象设计

> **现在**：一个 agent = 一堆代码。要改它的行为，改代码。
> **之后**：一个 agent = 一份配置。要改行为，改配置。

## 0. 现在缺什么

Agent Loop、工具调用层、会话持久化都有了。但这三块合起来只回答了一个问题：**一次任务怎么跑完、跑完怎么存下来**。

这个 agent 只有一个身份、一套工具、一个模型，而且全是写死的：

- 提示词是 `prompts.py` 里的两个常量
- 工具是启动时手工组装的
- 模型从环境变量读
- 预算是 `RunConfig` 的默认值

设计方案里的第一个核心对象 `Agent`——"描述一个 Agent 使用的模型、提示词、工具和策略"——**还不存在**。有 Runtime，没有可配置的 Agent。

---

## 1. 其实已经有两个 agent 了，只是它们是 if/else

`prompts.py` 里有两个常量：`SYSTEM`（单文件问答）和 `DIRECTORY_SYSTEM`（目录发现）。选哪个、配哪套工具、给多少预算，靠代码里的分支决定。

**这两个就是两个 agent。** 只是它们的存在形式是 if/else，不是两份配置。

这一批要做的第一件事，就是把这个 if/else 变成数据。

---

## 2. 配置装五样东西

| | 现在在哪 | 配置里应该是 |
|---|---|---|
| **身份 / 提示词** | `prompts.py` 两个常量 | 这个 agent 是谁、遵守什么规矩 |
| **能用哪些工具** | 启动时手工拼注册表 | 一份工具名单 + 每个工具的参数 |
| **用哪个模型** | 环境变量 | agent 自己说了算 |
| **预算策略** | `RunConfig` 全局默认值 | 最多几步、多长时间、读多少 |
| **审批策略** | 写死"medium 必问" | 哪些要问、能否"本次会话别再问" |

---

## 3. 产品上的变化：能定义多个助手

现在产品里只有**一个**助手，所有任务共用同一套提示词、工具和预算。

有了 Agent 对象，这个产品里自然会长出几个：

| Agent | 工具 | 预算 | 提示词重点 |
|---|---|---|---|
| **资料问答** | 只读 | 3 步 | 强调引用 |
| **报告生成** | 读 + 写 | 10 步，需审批 | 强调结构化输出 |
| **目录巡检** | 只读 | 大预算 | 强调找出变化和异常 |

**这三个的差别全部是配置差别，没有一行代码差别。**

现在这三个要么做不出来，要么得各写一份代码。

---

## 4. 边界：Agent 不是 Session

> **Agent = 我是谁** —— 静态、可复用、多个任务共享
> **Session = 我做过什么** —— 动态、一次性、属于某个 agent

现在有后者，没前者。

另外两条边界：

- **不是多 Agent 协作**。设计方案明确不做，这里也不做。
- **不是让用户写代码**。配置是声明式的，不是插件机制。

---

## 5. 为什么它必须排在 Skill / MCP 前面

**Skill** 的本质是：一段注入 Context 的指令 + 声明需要哪些工具。
→ 它注入到**哪个 agent** 的 context？装一个 Skill 是在改**哪份配置**？

**MCP** 的本质是：动态往注册表里塞工具。
→ 塞进**谁的**工具名单？权限算谁的？

没有 Agent 对象，Skill 无处可装，MCP 工具要么全局可见（危险）要么无处安放。这一步跳不过去。

---

## 6. 落地分两步

### 第一步：只搬家，不造新东西

把现有的两个模式表达成两份 Agent 配置，Runtime 只接受一个 Agent，删掉模式分支。

**验收：现有 387 个测试原样全绿。** 和工具层那次一样的标准。

这个切入点的好处是——它不是凭空造抽象，是**把已经存在的东西搬进去**。而且立刻能验证抽象对不对：**如果那两个模式表达不成配置，说明字段设计错了，当场就知道。**

### 第二步：配置能被管理

配置从文件读、能新建、能在界面上选。

**别一上来就做配置文件和 UI。** 先让第一步的抽象被现有代码验证过。

---

## 7. 一个会当场暴露的坑

`DIRECTORY_SYSTEM` 提示词里写着"最多成功读取 **4** 个不同文件"，同时代码里是 `DirectoryTools(max_files=4)`。

**同一个事实写了两遍。** 改一个忘了另一个，模型收到的规则就和程序实际执行的不一致。

这类地方在做配置时会被全部翻出来，因为配置里只能写一次。

**正确做法：预算写在配置里，提示词从配置渲染出来**，而不是手写一个数字进正文。

这会逼着 `prompts.py` 从"两段大文本"变成"模板 + 参数"。做完之后，提示词才第一次成为**可配置的东西**，Skill 也才有地方注入。

---

## 8. 这一批不做

- **多 Agent 协作、Planner**
- **Skill / MCP** —— 它们是这一步的下游，等配置对象稳定再说
- **Provider 抽象** —— 独立的一批，和这个不互相阻塞
- **Cron** —— 技术上已经很便宜，但价值依赖跨会话记忆，现在做只会每次得到一份一样的报告

---

## 9. 做完之后是什么状态

- 两个现有模式变成两份配置，代码里没有模式分支
- 加第三个 agent（比如"目录巡检"）**只需要加一份配置**
- 提示词里不再有硬编码的预算数字
- Skill 和 MCP 有了明确的安装位置

**一句话：这一步之前，这个项目是一个做得很好的 agent；这一步之后，它才开始是一个 agent 框架。**
