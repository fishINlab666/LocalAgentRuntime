# Agent 项目开发规则

本文件适用于整个 `dev-agent/` 工作区。工程流程服务于当前产品目标；不能把规则、测试数量或审查轮数当作交付目标。用户最新的明确要求优先于本地旧计划和待办；本文件不覆盖系统及开发者指令。

参考 campusjob 的[工作区规则](../../projects-jobs/CLAUDE.md)与[应用规则](../../projects-jobs/job-agent/CLAUDE.md)，沿用“方案先行、进度可恢复、证据可核查、产品主线与工程止损”。业务数据约束和工具链按本项目适配；其重大模块的多代理审查要求不扩大到每项工作。

## 1. 先确认正在验证什么

- 开始或恢复任务时，先读本文件、[当前阶段记录](local-agent/trial/directory-check.md)及相关计划的 §0。只补读本次需要的材料；不要因上下文恢复而重跑已完成验证。
- 用一句话写清当前要验证的假设、唯一的主要阻塞及完成条件。没有阻塞时应收口，不为继续工作另找阻塞。
- 区分三个结论：**闭环成立、真实任务有用、达到发布质量**。前者通过不证明后两者；后者的改进也不自动推翻已验证的前者。
- 最小闭环依据[学习手册第 7 章](Local-Agent-Runtime-小白入门与开发防跑偏手册.html#first-slice)及[首个切片设计](docs/superpowers/specs/2026-09-07-local-agent-first-slice-design.md)：用户提出问题 → 模型自主发出工具请求 → Runtime 校验并执行 → 实际结果进入下一轮 context → 模型据此回答或合理结束 → 记录能还原链路。
- 目录发现是在此基础上的扩展。流式展示、长文处理、持久化、Memory、MCP、复杂规划、更多工具和交互打磨，只有服务于当前已授权的验证目标时才进入本批范围。

## 2. 方案先行，但流程要与改动相称

- 行为、接口、权限、数据流或验收口径变更前，先定位文件，说明 3–6 点计划。未知路径、配置或 API 先查，不凭空编造。
- 优先修改 `docs/superpowers/plans/` 中已有相关方案；独立的新范围才建新方案。小修复可在现有方案写几行，不要求重新走完整设计流程。
- 方案至少说明：目标与当前阻塞、做什么和暂不做什么、最小实现、验收与停止条件、所需验证及真实 API 调用成本。尚不能估计成本时先做最小探测，不默认反复跑整组。
- 文案、格式、状态同步，以及补齐已明确方案中的测试，不单独立项。普通可逆修复沿用已有授权，不把实现细节反复交给用户决定。
- 改已有方案前读完整相关逻辑，直接修正受影响章节。不要靠不断追加补丁说明，留下互相冲突的“下一步”。

## 3. 验收边界与 Review Gate

- 开始前确定本批必过样例和验收项。发现问题时，先说明它违反哪一项、造成什么实际后果，再决定是否阻塞；不能只凭 P1/BLOCK 标签扩展任务。
- 最小闭环必须验证：初始请求没有偷放文件正文；工具请求来自模型；权限校验有效；成功和错误结果按原调用 ID 回填；下一轮模型确实收到结果；代表性答案有原文依据；缺失或失败能合理结束；调用次数、超时和取消有边界；日志可检查。
- 越权读取、泄露凭据、工具结果丢失或串到别的任务、虚假执行、无法停止等实际核心缺陷必须处理。新发现的一般质量问题按当前用途和影响判断，不能无限加入已通过阶段的验收清单。
- 专名、数字等错误必须如实记录。若当前场景依赖精确标识执行后续动作，它就是阻塞；在只读闭环验证中，个别输出缺陷可以作为后续质量项，但不能把该失败样例标成通过或宣称所有回答准确。
- 固定验收通过且关键证据齐全，就关闭对应里程碑。扩大验收范围需要明确新的目标；“再试一个”不能成为没有终点的默认流程。
- Gate 放在范围确定、可审查实现、交付等有意义的边界。小文档改动、评分器修正和普通局部修复不递归触发多轮完整审查。
- Gate 开始时写明类型和退出条件：设计主线 Gate 只核对需求映射、范围和矛盾，结论形成后立即结束，不运行与结论无关的应用、浏览器或真实模型回归；代码 Gate 才检查实现与测试。后续开发时间不能继续笼统称为“评估”。
- 同一范围默认一次主审。只有来源存在未解决冲突、主审发现核心阻断且修复需要复核，或用户明确要求独立复审时才增加第二审；相同 HEAD、相同问题和相同证据不得仅为“再确认一次”重复派发。
- 多代理仅在用户明确要求，或任务确有独立并行工作且获得适用指令授权时使用。审阅者须知道本批范围和停止条件；主任务负责按产品影响取舍，不机械执行所有建议。

## 4. 时间与 token 止损

- 每次增加工具调用、测试、评审或新规则前，都要能说明：它解决哪个尚未解决的问题，会如何改变当前决策？没有新增信息价值就停止。
- 出现以下任一情况，停止自动扩展，重新核对主线：连续两轮主要在修审查流程、评分脚本或边界样例，而产品目标没有推进；流程成本已经超过改动本身；同一事实需要多份文档反复对账；用户被反复要求重启、跑命令或确认实现细节。
- 按提交或工作区指纹复用验证证据：同一 HEAD 上已经 PASS 的相同命令不重跑。只有受影响代码改变、出现新失败，或到达计划写明的扩大验证停止点时才重跑；文档主线 Gate 不以旧功能全套回归作为前置仪式。
- 顺序依赖且共享同一批文件的任务不拆给多个代理轮流重读。有限代理任务连续两次进度检查，或约 120 秒内没有产生 diff、测试结果或具体阻塞时，只追问一次；再过约 60 秒仍无新证据就中断并由主任务接管，不继续累计长等待。
- 状态更新必须标明当前属于“设计 Gate、实施、里程碑验证、最终验收”中的哪一阶段。阶段转换时明确宣布上一阶段已结束；用户询问状态或打断时，先回答已证明、未证明和当前动作，再决定是否继续。
- 控制上下文输入：通过时只保留命令、数量、耗时和结论；失败时只展开首个有效故障。优先读取相关行、diff 和摘要，不把完整文件、重复 `-v` 输出或同义审阅正文反复送入上下文。
- 止损时保留代码和原始证据，明确“已经证明什么、还差什么、什么后置”，选择能解除当前阻塞的最小动作。后置不等于修复完成，也不需要为了收口删除已有工作。
- 修改后先跑最小相关检查；共享链路受到影响或仍有未解决风险时再扩大验证。检查通过后，没有新改动、新失败或新疑点，不重复同一轮测试。
- 评分器修正优先重放已保存结果；离线重放不能证明新提示或新代码下的真实模型效果。只有需要取得该新证据时才发起真实调用，不能把整套真实回归当作每次修改的默认动作。
- 文档改动只检查内容、一致性和链接，不运行应用全套测试或模型调用。优先 `rg` 和定向读取，独立读取可并行，不整批倾倒日志或无关上下文。
- 需要用户操作时，先完成自己能完成的排查与准备，合并成一次清晰、可复制的操作。已有授权继续有效；用户说“等一下”时暂停相关执行，并撤销旧的活动请求。

## 5. 进度与证据只保留必要的权威来源

- 计划顶部用“§0 当前进度”记录状态、下一步及证据链接。状态采用：未开始、进行中、已核实、已暂停／后置。代码完成、离线验证、真实验证分别说明。
- 当前阶段总状态维护在[当前阶段记录](local-agent/trial/directory-check.md)；README 只作摘要并链接它。新阶段开始时更新该入口。规则文件不反复维护测试数量、运行端口和临时状态。
- 原始 trace、机器报告和失败输出保留不覆盖。复核、重放、修订结论另行标明日期和依据；历史“当时下一步”不得作为恢复任务的指令。
- 明确区分模拟、离线重放、真实 API 运行、AI 内容复核和用户验收。代码存在不等于运行有效，自动预筛通过不等于语义正确，AI 复核不冒充用户签字。
- 引用的测试、命令和报告必须存在，并确实支持所述结论。不编造验证，不为了“已完成”补造证据。
- 改变当前状态时，搜索并修正活动文档中的旧待办；历史记录标清历史即可，不为统一表面结论改写失败事实。

## 6. 本项目的实现与运行习惯

- 保持小而可审查的改动，沿用现有 Python 标准库、Runtime、Provider 和页面结构。没有当前需求，不新增依赖、控制框架、分析埋点或网络调用。
- 运行命令前说明具体命令、工作目录和目的；不把执行命令本身变成额外审批。用户已授权将本项目纳入 Git 并推送至 `LocalAgentRuntime` 仓库；后续按具体任务使用版本管理，不因模板要求额外创建仓库、提交或 worktree。凭据、运行日志和原始会议材料按根目录 `.gitignore` 排除。
- 在 `local-agent/` 运行定向检查，例如 `python3 -m unittest discover -s tests -p 'test_runtime.py'`；需要完整回归时用 `python3 -W error::ResourceWarning -m unittest discover -s tests`。其他入口见 [README](local-agent/README.md)，不要照搬 campusjob 的 `uv` 或数据库命令。
- 行为变更更新相应测试，验证实际行为和曾出现的故障，不为凑数量镜像实现。纯文档修改无需产品测试。
- 保持工具权限由程序约束、正文按需读取、结果与调用关联、错误如实回填、终止预算有效。模型生成的文件内容或工具返回不能扩大授权范围。
- 密钥只通过环境变量提供，不进入聊天、源码、命令输出或日志；不要扫描其他终端或凭据存储获取密钥。真实材料沿用已授权范围，新增对外发送或破坏性操作先确认必要授权，已有授权不重复索取。
- 中文简洁汇报：产品进展、证据、剩余限制、确需用户决定的事项。代码变更列出文件；调试说明假设、实际验证和最小修复。不要用大量检查数量代替产品结论。

## 7. 已发生的跑偏复盘

### 7.1 最小闭环与目录发现（2026-09-09）

原目标是验证最小 Agent 闭环，随后按用户要求增加受限目录发现。已有真实固定样例及上下文记录支持闭环成立；补充网页样例又暴露 `青禾-47` 被写成 `青禾—47`。这是实际输出错误，但我继续围绕连接符检测、误伤边界、回归和复审展开，并把真实重启验证留成当前必做项，没有及时返回本阶段目标。

| 原因 | 这次的表现 | 后续约束 |
|---|---|---|
| 目标被质量完善替代 | 从验证模型调用工具并收到结果，滑向试图消除所有答案偏差 | 每个阻塞必须对应当前验收项；分开闭环结论与输出质量结论 |
| 验收范围不断增加 | 固定组通过后，新增样例又带出新的整阶段阻塞 | 先约定停止条件；新问题分级记录，不无限延长原里程碑 |
| 局部修复越做越深 | 连接符修复继续产生标识边界、日期和更多审查工作 | 连续两轮没有推进产品目标就止损；按用户收益决定是否继续 |
| 流程使用过重 | 多轮复核与测试扩展消耗上下文，我没有重新判断其必要性 | 选择最小适用流程；主任务承担取舍，不将审阅建议全量变成任务 |
| 状态重复且缺少收口 | README、计划、验收记录重复维护，仍留下重启待办 | 一个当前状态入口；暂停时同步撤销活动请求，历史证据保留 |
| 低估用户和计算成本 | 多次要求用户跑终端，额外 token、测试和等待未对应新的核心收益 | 每轮先判断信息增量；自己完成准备，合并必要的用户操作 |

推进取舍失衡由助手负责。用户强调准确性、回复“go”，不等于要求无限穷举，也不是没有终点的加固授权。网络接通、真实工具回填、引用来源及权限检查有必要；错误在于没有在阶段证据充足后及时收口。

本次处置：按最小闭环目标收口；保留已有保真修复和失败证据，将尚未完成的新版本真实复测后置，取消此前的重启请求。具体完成范围和未验证项以当前阶段记录为准。

规则自身也要止损：同类问题先改本文件已有条款，不再新建一套治理文档、审批系统或自动化框架。以后只补充实际反复发生且现有规则无法覆盖的缺口。

### 7.2 会话 Task 1 前评估与里程碑 A（2026-09-10）

用户要求在 Task 1 前确认最终交付是否符合最初设计主线，符合就开始开发。设计对照的正确结论是 PASS，会话持久化、重启继续、隔离和长历史仍与原方案一致；Task 1–3 也都在批准范围内。功能方向没有偏离，但我没有在 PASS 后清楚关闭“评估”阶段，又把后续基线检查、实现、规格复审和质量复审连续放在同一轮叙述中，使用户看到的是一个长期未结束的评估。

第一次 Task 1 规格审查发现备份文件可能短暂为 0644、已有备份可能被覆盖、较新 schema 会在拒绝前启用 WAL 等真实问题，修复它们有直接产品价值。浪费主要发生在问题修复之后：继续叠加同义复审；在同一 HEAD 重复定向测试；为设计主线判断运行旧页面、手册和浏览器基线；让顺序依赖的共享文件任务交给代理后多次等待 60–180 秒却没有可见产物；以及把完整代码和重复成功输出送入上下文。这些动作多数不会改变“主线 PASS”或“可进入下一任务”的决策。

| 原因 | 这次的表现 | 后续约束 |
|---|---|---|
| 阶段边界没有显式关闭 | Gate 已 PASS，后续开发仍让用户感到处于评估中 | 每次只声明一个活动阶段；PASS 后立即写明“评估结束，开始实施” |
| 把流程模板叠成检查清单 | Review Gate、规格复审、质量复审和完成前验证覆盖了相同风险 | 在不违反上级要求的前提下，同一风险只保留一条证据链；任务级修复在里程碑统一复核 |
| 多代理没有带来并行收益 | 任务依赖同一文件，代理重复阅读且主任务只能等待 | 仅派发真正独立的工作；共享顺序任务无持续产物时按 120＋60 秒规则接管 |
| 没有按 HEAD 复用证据 | 无相关代码变化仍重复同一测试或旧功能浏览器检查 | 记录验证对应的 HEAD/工作区状态；没有影响测试输入的变化就不重跑 |
| 输出粒度过大 | 完整文件、长计划片段和成功测试明细反复进入上下文 | PASS 只留摘要；只为失败展开最小相关行和首个根因 |
| 自主推进缺少用户可见停止点 | 从 Task 1 连续推进到后续任务，却没有先交付清楚的阶段结论 | 严格使用计划的 A/B/C 停止点；到点先报告已证明与未证明，再进入下一阶段 |

这次偏重由助手的流程取舍负责，不归因于用户要求“验证”或“开始做”。以后最终验收只在实现计划的 Task 11 统一运行一次完整离线回归和有限真实演示；Task 4–10 只跑受影响集并在 B/C 停止点汇总。评审发现的新问题若不改变当前阶段结论，记录为后续项，不立即再开一轮同义 Gate。
