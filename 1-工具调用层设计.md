# 工具调用层设计

> 一句话：工具调用层不是"把工具注册进去"的技术设施，**它是决定用户什么时候被打断、被打断时看到什么的地方**。这是它唯一的产品价值，其他都是副产品。

## 0. 为什么现在做这层

当前实现里，两个工具（`list_files` / `read_file`）硬编码在运行循环中，主循环靠 `isinstance(tool, DirectoryTools)` 分成两条路径，目录发现的授权状态（`scope`）直接糊在循环里。

这带来两个后果：

1. **加不了第三个工具**。每加一个就要再劈一次主循环。
2. **后面两步都卡在这**。会话持久化要存 ToolCall，可现在没有"Tool"这个对象，表结构没法定；写入功能要审批，可现在没有任何地方能挂"风险等级"。

而现在只有两个工具，**是重构成本最低的时刻**。等到五个再改，成本翻几倍。

---

## 1. 核心判断：工具现在只有两个受众，缺了最重要的第三个

一个工具要被调用，三方都得看懂：

| 谁 | 需要知道 | 现在有吗 |
|---|---|---|
| 模型 | 这工具干什么、参数怎么填 | 有 |
| 程序 | 怎么执行、限时多久、算不算预算 | 有 |
| **用户** | **它现在要干什么、后果多大、我要不要拦** | **没有** |

所以工具定义里除了给模型的描述，还得有一条**给用户的话**。同一个 `read_file`：

- 对模型说："读取工作区内已发现的 UTF-8 .md/.txt 文件"
- 对用户显示：**"读取 plan.md"**

这一条看着小，但它是审批、界面、日志全部的地基。没有它，审批弹窗只能写"是否允许 write_file？"——这种东西用户点三次就开始无脑点确认，审批就废了。

---

## 2. 一个工具声明五件事

| | 内容 | 谁看 |
|---|---|---|
| **名字** | `read_file` | 模型、日志 |
| **给模型的说明** | "读取工作区内已发现的 .md/.txt 文件" | 模型 |
| **给用户的一句话** | 带上实参 → **"读取 plan.md"** | 用户 |
| **风险等级** | 低 / 中 / 高 | 决定拦不拦 |
| **来源** | 内置 / 用户 / MCP | 界面显示，现在恒为"内置" |

加上一个 `执行` 方法。

**前五条是声明，只有最后一条是实现。** 现在全糊在实现里，这是要拆开的东西。

注意"给用户的一句话"是**带参数的**，不是固定文案。`write_file` 拿到实参才能说出"写入 summary.md（新建 1.2 KB）"。

---

## 3. 三个角色，各管各的

**工具** —— 声明自己、执行自己、**记自己的账**

已列过哪些目录、已读几个文件、预算还剩多少。这些现在在主循环里，搬回工具自己身上。

**注册表** —— 装着这轮可用的工具，对外只提供三件事：

- 给模型一份 schema 清单
- 按名字找到工具
- 汇总一份"本轮状态摘要"（就是现在的 `scope`，但由各工具报上来，主循环只负责转发）

**主循环** —— 四步，**不认识任何具体工具**：

问模型要动作 → 该拦就拦 → 执行 → 结果回填

---

## 4. 一次调用走五道关卡

```
模型发起调用
   │
   ├─① 这工具存在吗 ──────────── 否 → 信封「未知工具」
   ├─② 参数合法吗 ────────────── 否 → 信封「参数错误」
   ├─③ 风险要拦吗 ────────────── 是 → 暂停，给用户看"要干什么"
   │                                      ├─ 拒绝 → 信封「用户拒绝」
   │                                      └─ 同意 → 继续
   ├─④ 执行（带超时） ─────────── 失败 → 信封「执行失败」
   └─⑤ 工具更新自己的账
   │
结果回填（统一信封）+ 记一条过程日志
```

**①②④⑤ 现在都有，只是散在主循环里。③ 完全没有。**

整个改造的实质就一句话：**把 ①②④⑤ 收拢到一个地方，给 ③ 腾出位置。**

---

## 5. 结果信封

```
成功  { ok: true,  data: {...} }
失败  { ok: false, error: { 码, 人话, 归谁 } }
```

`归谁` 是新增的关键字段，只有两个值：

| 值 | 含义 | 处理 | 例子 |
|---|---|---|---|
| `模型` | 模型自己能绕过去 | 回填给模型，**不打断用户** | 文件不存在、格式不支持、超预算 |
| `用户` | 模型再聪明也解决不了 | **立刻停下告诉用户** | 没配密钥、没有权限、磁盘满 |

当前实现把所有错误都按"模型"处理，结果是环境问题会变成模型空转几轮、最后吐一句模糊的"无法完成"。**加这一个字段就解决了。**

---

## 6. 风险分级：按"能不能撤销"分，不按"听起来危不危险"

这是产品决策，不是技术决策。判据只有一个：**做完之后能不能回到原样**。

| 等级 | 判据 | 例子 | 默认策略 |
|---|---|---|---|
| 低 | 完全可逆，不改变任何东西 | 列目录、读文件、搜索 | 直接执行，不打断 |
| 中 | 改变了东西，但看得见、撤得回 | 写新文件、改文件 | 确认一次，可记住 |
| 高 | 撤不回来，或影响外部 | 删除、覆盖、执行命令、发消息 | 每次都确认 |

关键的产品张力：**打断太多没人用，打断太少不敢用。**

所以中风险要允许"这个目录下的写入，本次会话不用再问我"——把选择权交给用户，而不是产品替他决定。

---

## 7. 确认的时候给用户看什么

用户批准的是**后果**，不是工具名。

**不要这样：**

> Agent 想要调用 write_file，是否允许？

**要这样：**

> **写入 `docs/summary.md`（新建，1.2 KB）**
>
> ```
> ## 项目结论
> 代号：青禾-47
> 评审人：……
> ```
>
> [看全部] [确认] [拒绝]

如果是覆盖已有文件，必须明说"**会覆盖现有的 3.4 KB 内容**"，最好给个 diff。

**审批的价值 100% 取决于你展示了什么，而不是有没有这个环节。**

---

## 8. 三个工具落地后长什么样

| | list_files | read_file | write_file |
|---|---|---|---|
| 给用户的话 | "查看 docs/ 里有什么" | "读取 plan.md" | "写入 summary.md（新建 1.2 KB）" |
| 风险 | 低 | 低 | **中** |
| 自己记的账 | 已列目录 | 已读文件、文件预算 | 已写文件 |
| 打断用户吗 | 不 | 不 | **会，带内容预览** |

`write_file` 就是验证这套设计的试金石——**它必须能"只写声明就接上"**。

---

## 9. 改造分三步，每步都能跑

### 第一步：只搬家，不改行为

两个现有工具按新格式声明一遍，主循环里的 `isinstance` 分支删掉，改走注册表。

- **验收：现有 200 个测试原样全绿，一个都不改。** 绿了说明搬对了。
- **这一步不许加任何新功能。** 这是最容易失守的地方。

### 第二步：加"归谁"字段 + 用户可见文案

界面上把"读取 plan.md"显示出来。

- **验收：用户第一次看到 agent 在干什么**，而不是一个转圈的进度条。

### 第三步：加 write_file，验证抽象

- **验收：主循环一行没改、审批自动弹出来了、错误自动归类了。**
- **如果要改主循环才能接上 → 抽象错了，回第一步重来**，不要打补丁。

---

## 10. 一个要提前说好的妥协

审批意味着"运行暂停、等用户"。**这个暂停状态现在没地方存**——进程一关就没了。

**接受这个降级**：审批只在同一次运行内有效，进程活着就行，不跨重启。

不要为了"审批能跨重启"把会话持久化提前做——那正好是上次跑偏的模式：为了一个边界情况，把下一阶段的工作拽进当前阶段。**跨重启的审批等持久化做完自然就有了。**

---

## 11. 停在哪

第三步做完就停。

**不做：** MCP 接入、Skill、用户自定义工具注册、更多内置工具。

**但留两样：** `来源` 字段、界面上显示来源的位置。成本几乎为零，不留的话接 MCP 时整层重做。
