# 单文件页面真实材料试用

状态：第三轮真实三问 3/3 通过程序检查及独立 AI 内容复核；旧四类基准重新执行 12/12 通过，主任务已逐条核对语义。单文件技术闭环通过这一组验证。2026-09-08 用户明确要求暂缓交互细化，以输入、回答准确性和工具结果回填作为重点，并授权进入目录发现；额外主观体验反馈不再是本批前置条件。前两轮 FAILED 报告保留，未覆盖或改记成功。

启动核验：最初 8765 未启动（终端粘贴导致 `cd: too many arguments`）；改为单行 `cd ... && python3 ...` 后已成功启动，三次均使用 DeepSeek 真实模式且成功读取节选。

## 第一轮结果与修复

- 报告：`results/real-web-683982da-b73f-4033-9f0a-a3a24814a2cb/report.json`，保留原始 `FAILED` 结果。
- 事实查询 `7ffa9200d22d4ec89c134831dd50e2e5`：`INVALID_CITATION`，2 次模型调用，2.4383 秒。
- 步骤整理 `73cc3ade7dd54d1a8808f8b412407898`：`INVALID_CITATION`，3 次模型调用，13.0897 秒；期间进行过一次 JSON 格式修复。
- 缺失信息 `aef154e688d544298c12206701201ce1`：`not_found`，2 次调用，2.0291 秒；正确说明节选未给出验收日历日期和具体负责人。
- 单次诊断 `50624a405f0541c1a0ba0b04902f5c4b` 明确复现：模型把第 7 行指为第 1 行，且将“啊，就接一个api啊”改成“就是接一个api啊”，截掉行首、行尾空格。不是网络或 Key 问题，校验拒绝符合原约定。
- 修复：Runtime 优先对发给模型的 `content` 编成行号到原文的映射，保留空行及首尾空格；原始快照和严格引用校验不变。提示词使用通用示例，明确真实引用必须取自本次工具结果。若编号后的完整请求超出 64 KiB，则回退完整原文；原文请求仍超限才停止，不截断、不扩大预算。极多短行的回退模式仍需模型数行，引用错误仍会被拒绝。
- 本地验证：108 项单元/HTTP 测试通过；浏览器交互及五种窗口尺寸通过。含先失败后通过的编号正文回归，以及错行、改写、漏空格仍拒绝的检查；4000 行单次读取和 5000 行同批两次读取均可在原预算内降级，原文本身超预算仍停止。
- 独立审阅：发现并修复编号导致短行文件超预算的问题；复审 PASS，31 项 Runtime 测试独立重跑通过，无剩余明确问题。
- 尚待完成：修复后的真实三问、逐项语义核对及用户体验反馈。测试替身通过不代表真实模型已修复。

## 第二轮结果与引用位置协议

- 报告：`results/real-web-d5a3bb57-4f39-4f6b-b17d-835c6693244c/report.json`，保持原始 `FAILED`。三次日志的系统提示摘要均与当时最新代码一致，排除服务仍加载旧版本。
- 事实查询 `cda4e5410c884c8cb4f2a9687f4b1214`：`INVALID_CITATION`，2 次模型调用，3.4564 秒。默认日志不含原始回复，不能进一步断言此次具体是哪一处行号或摘录不匹配。
- 步骤整理 `4b670d3e564a48efaced5b36205f4f16`：`answered`，3 次调用，7.6081 秒；八处引用均精确匹配。语义核对覆盖 API、读取、上下文、loop 和暂不做的内容，但“均为尚未执行”比原文证据更强，应改成“未记载已完成”；`collect/code` 的自行补词也应避免。
- 缺失信息 `a155c9717ae841b4969fa60126b4c5c5`：`not_found`，3 次调用，2.7702 秒；正确说明未提供验收日期和负责人。页面刷新恢复成功。
- 新协议：模型只提供 `path/start_line/end_line`；程序确认本次成功读取、路径和整数范围，再从已有快照提取完整行，包括空行及首尾空格。模型如果提交了 `quote`，仍须完全匹配，不自动修补错误摘录。不重新打开文件，不修改答案文本，用户看到的引用结构保持不变。
- 能力边界：范围内但与答案无关的行号仍可能通过来源检查；原文真实不能自动证明支持结论。真实三问必须再次逐项核对语义，不能只看程序 completed。
- 本地验证：112 项单元/HTTP 检查和浏览器回归通过；独立复审 PASS，26 项答案测试与32 项 Runtime 测试另行重跑通过。
- 后续：服务加载新协议后重跑真实三问，并验证正常读取、缺失信息、文件不存在、换内容四类旧基准，随后收集实际页面体验反馈。

## 第三轮通过与当前状态

| 验证 | 结果 | 证据 |
|---|---|---|
| 真实会议节选三问 | 3/3 程序检查与内容复核通过；9 处引用一致，耗时 2.201 / 5.9372 / 1.4697 秒 | [三问复核](results/real-web-87f4e1ec-749e-4fdc-9b03-4cbf99056ec8/semantic-review.md) |
| 四类固定基准 | 正常问答、信息缺失、文件不存在、换内容各 3 次，12/12 通过并逐条核对 | [基准复核](results/baseline-web-bbf0143ea8fd44af9b9f0b0a2c7de917/semantic-review.md) |
| 页面 | 真实答案与引用可见，长文引用卡片无遮挡，刷新恢复成功；合成测试另覆盖取消、错误与窄屏 | 三问报告及截图、本地浏览器回归 |
| 用户体验 | 待反馈；已给出真实 8765 页面、文件名和事实查询题，询问路径填写与引用核对是否方便 | 当前任务的试用反馈问题 |

复核者为 Codex 主任务及独立 AI 审阅，不冒充用户或工程师人工签字。机器报告的待复核状态保留原值，另附 Markdown 复核结论。

非阻断体验问题：步骤整理偏长；“既有事实”标题实际包含计划与表态，应避免误读；转写名称不应擅自合并。它们已记录在三问复核中，不通过反复修改基准或覆盖失败结果掩盖。

已验证的最小产品：指定一份小文本 → 提问 → 模型自主读取 → 根据本次内容回答 → 查看可回查原文的引用。下一批目录发现按用户新要求实施，独立记录代码、模拟测试和真实模型验收状态，不将本轮单文件结果冒充多文件验证。

重跑命令（在 local-agent 下；服务应已使用 trial/workspace 启动）：`PYTHONPATH=. python3 trial/run-web-baseline.py`。基准只在该工作区内创建并清理自己的临时合成文件，结果写在工作区外，不影响真实节选。

## 输入与边界

- 输入：workspace/meeting02-excerpt.md，约两分钟的连续会议原文转写；源文件为 meeting/0829/会议录制：agent-meeting02.md，00:19:24–00:21:13。
- 保留口语及转写词（如 refile、tour list），不把整理后的方案当成原始材料。
- 工作区仅为 trial/workspace；本文件及结果记录在该工作区之外，模型不会看到核对标准。
- 本轮只验证这一节选，不能声称已经综合完整会议或三份原始材料。
- 使用 DeepSeek 真实模式运行三次独立问答，记录 run_id、最终状态、答案、引用、耗时、模型调用数和页面截图；不保存或输出 API Key。

## 三个问题与核对标准

1. 问题：会议建议先验证 API 的什么输入输出过程，再做什么工具？请引用原文。
   - 必须覆盖：先接 API，发送 input、等待 output；随后做读取文件工具（原文转写 refile）。不能说已经接通或已经开发完成。
   - 依据：00:19:24 原文。
2. 问题：按会议原文整理接下来要做的步骤，以及当时明确说先不管的内容；区分会议建议和已经完成的事实，引用原文。
   - 必须覆盖：接 API → 文件读取验证 → 上下文历史拼入 → 跑通 loop；明确“system prompt memory and memory，这个先不管”。可说明七八个常见工具是会议中的建议，不得倒写为当前已实现数量。
   - 不得自行加入日期、负责人、已完成结论，或以本项目后续方案替代会议口头表述。
3. 问题：这段会议是否明确给出了项目验收的日历日期和具体负责人？若未说明，请明确指出，不要根据录制时间或说话人推断。
   - 必须为 not_found，说明节选没有明确日历日期或具名负责人；不把时间戳当截止日期，不编造人名。

## 体验核对

- 技术：真实 Provider（simulated=false），每次真实调用 read_file，再返回本次文件引用；失败不得改记为通过。
- 内容：逐项核对上述标准、引用行号和摘录，记录错误与修改量。
- 页面：填入路径和问题、提交、等待、展开引用、复制、刷新恢复；不将自动化操作耗时当成人类节省时间。
- 用户反馈尚待收集：路径填写是否麻烦、原文是否方便核对、是否愿意再次使用。自动化检查不能替代此项。
- 原先要求额外体验反馈后再进入目录；该前置条件已被用户 2026-09-08 的明确指令替代。交互收益仍未测量，但不阻挡目录与上下文链路开发。

## 启动

在已有 DEEPSEEK_API_KEY 的终端执行（不带 --demo）：

```zsh
cd "/Users/wujingyu/Desktop/AI/projects/dev-agent/local-agent" && python3 -m local_agent serve --workspace trial/workspace --port 8765 --open
```

不要把 Key 发送到聊天；启动后由当前任务继续完成三次网页问答和原文核对。
